{
  "name": "really-ai-whatsapp-bridge",
  "version": "2.0.0",
  "description": "whatsapp-web.js bridge for really.ai v2 — forwards inbound WA messages to the Python FastAPI backend and exposes HTTP endpoints for sending messages and initiating calls.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
