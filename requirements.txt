# really.ai v2 — Python dependencies
# SQLModel / SQLAlchemy replaced by Beanie (ODM) + Motor (async driver).
# All other deps from v1 are preserved.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# MongoDB
beanie>=1.26.0
motor>=3.4.0

# Configuration
pydantic-settings>=2.2.1

# OpenAI — GPT-4o chat + text-embedding-3-small embeddings
openai>=1.30.0

# HTTP client (used by whatsapp.py and vapi.py service layers)
httpx>=0.27.0

# Resend — email introductions
resend>=2.0.0

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.9
aiofiles>=23.0.0
rich>=13.7.0
